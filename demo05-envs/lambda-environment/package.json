{
  "name": "lambda-environment",
  "version": "1.0.0",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "invoke-local": "sls invoke local --stage prod -f commit-message-scheduler --logger",
    "invoke": "sls invoke -f commit-message-scheduler --logger"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "aws-sdk": "^2.714.0",
    "axios": "^0.19.2",
    "cheerio": "^1.0.0-rc.3",
    "env-var": "^6.2.0"
  }
}
